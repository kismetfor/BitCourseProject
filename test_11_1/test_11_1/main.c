#define _CRT_SECURE_NO_WARNINGS 1
#include <stdio.h>

int main() {
	//【一维数组】BC100-有序序列合并
    int n, m;
    while (scanf("%d %d", &n, &m) != EOF) { // 注意 while 处理多个 case
        // 64 位输出请用 printf("%lld") to 
        int arr1[n];
        int arr2[m];
        printf("%d\n", a + b);
    }
	return 0;
}